<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>UltimatePanel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-black border-bottom border-purple">
    <div class="container">
        <a class="navbar-brand" href="/">
            <span class="brand-ultimate">ULTIMATE</span><span class="brand-panel">PANEL</span>
        </a>
        <div class="collapse navbar-collapse show">
            <ul class="navbar-nav ms-auto">
                {ifset $currentUser}
                    <li class="nav-item"><a class="nav-link" href="{link Homepage:default}"><i class="fa-solid fa-gauge"></i> Dashboard</a></li>
                    <li class="nav-item"><a class="nav-link" href="{link Server:default}"><i class="fa-solid fa-server"></i> Servery</a></li>
                    {if $currentUser['role'] === 'admin'}
                        <li class="nav-item"><a class="nav-link" href="{link Admin:default}"><i class="fa-solid fa-shield"></i> Admin</a></li>
                    {/if}
                    <li class="nav-item"><a class="nav-link" href="{link Sign:out}"><i class="fa-solid fa-right-from-bracket"></i> Odhlásit</a></li>
                {else}
                    <li class="nav-item"><a class="nav-link" href="{link Sign:in}">Přihlásit</a></li>
                    <li class="nav-item"><a class="nav-link" href="{link Sign:register}">Registrace</a></li>
                {/ifset}
            </ul>
        </div>
    </div>
</nav>

<main class="container py-4">
    {foreach $flashes as $flash}
        <div class="alert alert-{$flash->type} shadow-sm">{$flash->message}</div>
    {/foreach}

    {include content}
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
